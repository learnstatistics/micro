\documentclass[letter,12pt]{book}

% Changes the font of the book
\usepackage{mathptmx}
\usepackage[T1]{fontenc} %%Lyryx Icon
\usepackage{roboto} %%for Lyryx Icon font


\usepackage{amssymb}
\usepackage{amsmath}

\usepackage[EULERGREEK]{sansmath}

% Make it look more like the Word docs
\setlength{\parskip}{\baselineskip}
\setlength{\parindent}{0pt}

% Coloring various things
\usepackage[usenames,dvipsnames,table]{xcolor}

% Computing dimensions of things
\usepackage{calc}

% Conditional statements
\usepackage{ifthen}

% Page layout
\usepackage[headheight=15pt,top=1in, bottom=1in, outer=1in, inner=1in]{geometry}

% Figures that don't float
\usepackage{float}

% Used to define new float type for Application Boxes
\usepackage{newfloat}

% Wrapping text around a figure
\usepackage{wrapfig}

% Better version of cline
\usepackage{hhline}

% Coloring table cells
\usepackage{colortbl}

% Colored tables and other nice enhancements
\usepackage{tabu}

% For multirow cells in tables
\usepackage{multirow}

% For multicolumn cells in tables
\usepackage{multicol}

% Accompanies titlesec for handling toc/lof/lot entries
% TODO: \usepackage{titletoc}

% Customized part, chapter, section & subsection headers
\usepackage[explicit]{titlesec}

% For more caption features. The options make the captions bold.
\usepackage[font=bf]{caption}

% Change numbering style of enumerate
\usepackage{enumerate}

% Change spacing in and around lists
\usepackage[shortlabels]{enumitem}
\setlist{topsep=0pt,partopsep=0pt,parsep=0pt,itemsep=5pt}

\usepackage{tcolorbox} %%for cover page 

% Draw frames around text
\usepackage[framemethod=tikz]{mdframed}

% For printing counters in English
\usepackage{fmtcount}
%\FCloadlang{USenglish}

% Hyperlinks in PDF
%\usepackage[bookmarksnumbered,breaklinks]{hyperref}
\usepackage{hyperref}

% To prevent empty pages from having headers and footers
\usepackage{emptypage}

% Used on title page
\usepackage{pagecolor}
\usepackage{afterpage}

% Draw images and figures
% New versions of pgfplots load tikz automatically and conflict with
% us loading it here...
%\usepackage{tikz}

% For plotting data
\usepackage{pgfplots}

\usetikzlibrary{calc,intersections,shapes.callouts}
\usetikzlibrary{decorations.text}
\usetikzlibrary{shapes,positioning}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{shadows,trees}
\usetikzlibrary{backgrounds}

\usepackage{pgfplotstable}

% For fancy headers!
\usepackage{fancyhdr}

% Used for creating EOC questions with corresponding solutions at the end of the textbook
\usepackage{answers}


% Inputs information regarding book Title, Authors, Version and Revision
\input{bookinfo.txt}

% Inputs information regarding Institution,Course Code, Name, Session and Section (for adapted text)
\input{courseinfo.txt}

\newcommand{\textbookid}{CURTIS_PRIN_MIC_1}
\newcommand{\epubuid}{3493ab34-ebed-48fe-8cb3-bf6164ff1dce}


\usepackage{prineconcommon}
\usepackage{prineconmicro}



\newlength{\coverwidth}
\newlength{\frontsideleft}
\newlength{\pagethickness}

\newcommand{\numpages}{60}

% Color interior
\setlength{\pagethickness}{0.0025in}

% B+W Interior
%\setlength{\pagethickness}{0.002252in}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Geomeotry package helps with creating a custom page size for a cover
%
%%% Here are the calculations from createspace:
% 
% Spine width: 0.002252 * page count = 0.691364  --> black+white interior
%              --> color interior is 0.0025 * page count
%
% Spine: allow for 0.0625" variance on either side of the fold lines for your cover.
%
% Bleed: 0.125 * 2  (needed on all 4 outside edges of PDF, but not spine)
%
% Safe zone: Text and images must be at least .125" inside the trim lines to ensure
%            that no live elements are cut during the bookmaking process.
%
% Minimum Cover Width: Bleed + Back Cover Trim Size + Spine Width + Front Cover Trim Size + Bleed
%
% 
% For 454 pages in color:
%
% paper width = 0.125 + 8.5 + (0.0025 * 454) + 8.5 + 0.125 = 18.385in
% paper height = 11 + (2 * 0.125) = 11.25in
%
%                paper size - safe zone
% layout width = 8.5 - (.125 * 2)
% layout height = 11 - (.125 * 2)
%
%                   bleed + left side + spine + safe zone
% layout offset x = 0.125 + 8.5 + (0.0025 * 454) + 0.125 = 9.885in
%                   bleed + safe zone
% layout offset y = 0.125 + 0.125 = 0.25in
%
%
% HOWEVER this will leave space for headers/footers/marginal notes/etc
%
% So we want to tweak the body size to be something nice, such as layout minus 1/2 inch on each side for margins
%

%%%%%%%%%%%%%%%%%%%%%%%
% For 329 pages in B+W:
%
% paper width = 0.125 + 8.5 + (0.002252 * 329) + 8.5 + 0.125 = 17.985908in
% paper height = 11 + (2 * 0.125) = 11.25in
%
%                paper size - safe zone
% layout width = 8.5 - (.125 * 2)
% layout height = 11 - (.125 * 2)
%
%                   bleed + left side + spine + safe zone
% layout offset x = 0.125 + 8.5 + (0.002252 * 329) + 0.125 = 9.490908in
%                   bleed + safe zone
% layout offset y = 0.125 + 0.125 = 0.25in

% Options showframe, showcrop, and verbose can be used for debugging

% Color
%\geometry{papersize={18.0725in,11.25in},layoutsize={8.25in,10.75in},layoutoffset={9.5725in,0.25in},body={7.25in,9.75in}}
% B+W
%\geometry{papersize={17.985908in,11.25in},layoutsize={8.25in,10.75in},layoutoffset={9.490908in,0.25in},body={7.25in,9.75in}}

%%Create Space
%\setlength{\coverwidth}{0.125in + 8.5in + (\pagethickness * \numpages) + 8.5in + 0.125in}
%\setlength{\frontsideleft}{0.125in + 8.5in + (\pagethickness * \numpages) + 0.125in}

%LULU
\setlength{\coverwidth}{44.152cm}
\setlength{\frontsideleft}{22.548cm} %Spine width + Spine begins +safe zone

\geometry{papersize={\coverwidth,11.25in},layoutsize={8.25in,10.75in},layoutoffset={\frontsideleft,0.25in},body={7.25in,9.75in}}


\begin{document}

	\pagestyle{empty}
	\input{frontmatter/titlepage}

\end{document}
